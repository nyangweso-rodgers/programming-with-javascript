//SurveyFormHeader

const SurveyFormHeader = () => {
  return (
    <>
      <header>
        <nav className={`navbar navbar-expand-sm`}>
          <div className={`container`}>
            <span className={`navbar-brand`}>App Header</span>
          </div>
        </nav>
      </header>
    </>
  );
};

export default SurveyFormHeader;
