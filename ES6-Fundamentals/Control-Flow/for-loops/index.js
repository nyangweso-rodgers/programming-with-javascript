// for loop in ES6

let arr = [1, 2, 3];
        for (let i = 0; i < arr.length; i++) {
            console.log(arr[i]);
        }

        let obj = {a:1, b:2, c:3};
        for (let v in obj) {
            console.log(v);
        };